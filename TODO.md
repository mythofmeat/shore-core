- [ ] if an active conversation gets compacted, there is no longer any anchor point for the character/model to know what the date/time is. it might be worth forcing the date/time into the system prompt somewhere (which *should* actually be cache-safe now since the system prompt only changes on compaction?) or just force the exact time-gap timestamp thing to occur on the first user message after compaction (probably easier. less risky)

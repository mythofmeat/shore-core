# Claude Entry Point

Use [AGENTS.md](AGENTS.md) as the authoritative agent entry map. This file exists
for tools that look for `CLAUDE.md`; it intentionally points to the shared map
instead of duplicating guidance.

If only this file is injected into context, open `AGENTS.md`, then follow its
links to the relevant source-of-truth docs.

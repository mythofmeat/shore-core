name: release-plz

on:
  push:
    branches: [main]

# Split into two jobs per the release-plz quickstart: only the release-PR
# job is wrapped in a concurrency group. Wrapping the `release` step in
# concurrency too can drop a pending tag/GitHub-release run when a newer
# main push arrives, which would skip the package.yml tag build for an
# already-merged release PR.

jobs:
  release-plz-release:
    name: release-plz release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.REPO_ARCH_TOKEN }}
      # Match the toolchain in protocol-guardrails.yml: `.cargo/config.toml`
      # forces clang + `-fuse-ld=mold`, so cargo verify (run by release-plz
      # during version determination) needs both available on the runner,
      # plus the same system deps the rest of the workspace builds against.
      - name: Install system build dependencies
        run: sudo apt-get update && sudo apt-get install -y mold libasound2-dev protobuf-compiler
      - uses: dtolnay/rust-toolchain@stable
      - uses: release-plz/action@v0.5
        with:
          command: release
        env:
          GITHUB_TOKEN: ${{ secrets.REPO_ARCH_TOKEN }}

  release-plz-pr:
    name: release-plz PR
    runs-on: ubuntu-latest
    concurrency:
      group: release-plz-${{ github.ref }}
      cancel-in-progress: false
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.REPO_ARCH_TOKEN }}
      # Match the toolchain in protocol-guardrails.yml: `.cargo/config.toml`
      # forces clang + `-fuse-ld=mold`, so cargo verify (run by release-plz
      # during version determination) needs both available on the runner,
      # plus the same system deps the rest of the workspace builds against.
      - name: Install system build dependencies
        run: sudo apt-get update && sudo apt-get install -y mold libasound2-dev protobuf-compiler
      - uses: dtolnay/rust-toolchain@stable
      - uses: release-plz/action@v0.5
        with:
          command: release-pr
        env:
          GITHUB_TOKEN: ${{ secrets.REPO_ARCH_TOKEN }}

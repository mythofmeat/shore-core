name: Test reusable
on:
  push:
    branches: [main]
jobs:
  call:
    uses: mythofmeat/repo-arch/.github/workflows/hello.yml@main
    with:
      pkgbuild_dir: contrib
      extra_pacman_packages: "alsa-lib protobuf"
    secrets: inherit

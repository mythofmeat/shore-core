{
  "name": "@shore/llm-sidecar",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "build": "bun build src/server.ts --outfile dist/shore-llm-sidecar --minify --target=bun --banner='#!/usr/bin/env bun' && chmod +x dist/shore-llm-sidecar",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.100.1",
    "@google/genai": "^2.7.0",
    "@openrouter/sdk": "^0.12.79",
    "openai": "^6.39.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
